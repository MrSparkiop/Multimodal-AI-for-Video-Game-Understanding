# GameSense - Multimodal AI for Video Game Understanding
# ------------------------------------------------------
# CPU-only installation works out of the box.  For CUDA, install the matching
# torch/torchvision wheels first, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# and then run `pip install -r requirements.txt`.

# --- deep learning -----------------------------------------------------------
torch>=2.1
torchvision>=0.16
transformers>=4.40,<5
huggingface_hub>=0.24

# --- data --------------------------------------------------------------------
numpy>=1.24
pandas>=2.0
pyarrow>=14
scikit-learn>=1.3
Pillow>=10.0
requests>=2.31
tqdm>=4.66

# --- visualisation -----------------------------------------------------------
matplotlib>=3.7
seaborn>=0.13

# --- application -------------------------------------------------------------
streamlit>=1.30

# --- testing -----------------------------------------------------------------
pytest>=7.4

# --- notebook (optional but needed to execute notebooks/) --------------------
jupyterlab>=4.0
ipykernel>=6.29
nbformat>=5.9
